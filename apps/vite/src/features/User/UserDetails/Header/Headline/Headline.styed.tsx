import styled from 'styled-components';

const Container = styled.div`
  color: white;
  margin-left: 200px;
`;

const NameHeader = styled.h3`
  margin: 0;
`;

const JobHeader = styled.p`
  margin: 0;
`;

export { Container, NameHeader, JobHeader };
